cmake_minimum_required(VERSION 2.6)
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Release)
endif()

add_subdirectory(makelib)
add_subdirectory(rc)
add_subdirectory(3rdparty)
add_subdirectory(rdm)
add_dependencies(rdm leveldb clang llvm)
